const Subtitle = ({ text }: any) => {
  return (
    <h5 className="text-btnText text-bgSecondary uppercase font-fontBold text-center md:text-left ">
      {text}
    </h5>
  );
};

export default Subtitle;
